<form method="post" action="{{ path('seance_delete', {'idseance': seance.idseance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
<form method="post" action="{{ path('seance_delete', {'idseance': seance.idseance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input class="btn btn-danger "  type="hidden" name="_token" value="{{ csrf_token('delete' ~ seance.idseance) }}">
    <button class="btn btn-danger " >Delete</button>
</form>
